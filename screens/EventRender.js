import React from 'react;
import { View, Text, StyleSheet} from 'react-native';

const Evenrender  = props => {
    return (
        <View>

        </View>
    );
};

const styles = StyleSheet.create({
    screen:{
        flex: 1,
        padding: 10,

    }
});

function render(Text, file) {
    
}